findAcType =>

SELECT COUNT(CUSTOMER_ID) FROM CUSTOMER WHERE EMAIL_ID=:email_id;

SELECT COUNT(SELLER_ID) FROM SELLER WHERE EMAIL_ID=:email_id;

categoryList =>

SELECT CATEGORY_ID, CATEGORY_NAME FROM CATEGORY;

returnSignUp =>

SELECT MAX(CUSTOMER_ID) FROM CUSTOMER;

returnSellerSignUp =>

SELECT MAX(SELLER_ID) FROM SELLER;

returnLogin =>

SELECT PASSWORD FROM CUSTOMER WHERE EMAIL_ID=%s;

returnSellerLogin =>

SELECT PASSWORD FROM SELLER WHERE EMAIL_ID=%s;

returnCustomerHome =>

SELECT FIRST_NAME, LAST_NAME, ADDRESS, PHONE_NUMBER, 
TO_CHAR(DOB,'dd MONTH, yyyy'), EMAIL_ID 
FROM CUSTOMER WHERE EMAIL_ID= :email_id;

returnSellerHome =>

SELECT NAME, ADDRESS, PHONE_NUMBER,  EMAIL_ID, WEBSITE, 
BALANCE FROM SELLER WHERE EMAIL_ID= :email_id;

returnDeliveryPending =>

SELECT EMPLOYEE_ID FROM EMPLOYEE WHERE EMAIL_ID= :del_guy_email;

returnAdminHome =>

SELECT (FIRST_NAME || ' ' || LAST_NAME), EMAIL_ID, SALARY, PHONE_NUMBER, ADDRESS 
FROM EMPLOYEE WHERE EMAIL_ID =:email_id;

returnCusCareHome =>

SELECT (FIRST_NAME || ' ' || LAST_NAME), EMAIL_ID, SALARY, PHONE_NUMBER, ADDRESS 
FROM EMPLOYEE 
WHERE EMAIL_ID =:email_id;

returnDeliveryHome =>

SELECT (FIRST_NAME || ' ' || LAST_NAME), EMAIL_ID, SALARY, PHONE_NUMBER, ADDRESS 
FROM EMPLOYEE 
WHERE EMAIL_ID =:email_id;

returnHireCusCare =>

SELECT COUNT(*) FROM EMPLOYEE WHERE EMAIL_ID =:email_id;

SELECT MAX(EMPLOYEE_ID) FROM EMPLOYEE;

returnHireDeliveryGuy =>

SELECT COUNT(*) FROM EMPLOYEE WHERE EMAIL_ID =:email_id;

SELECT MAX(EMPLOYEE_ID) FROM EMPLOYEE;

